{
  "name": "lucky-capsule-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Lucky Capsule - A daily capsule game on Base Network",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy-contracts.js --network baseSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy-contracts.js --network base",
    "verify": "hardhat run scripts/verify-contract.js --network base",
    "fund": "hardhat run scripts/fund-pool.js --network base",
    "balance": "hardhat run scripts/check-balance.js --network base",
    "test": "hardhat test"
  },
  "keywords": ["ethereum", "solidity", "hardhat", "base", "smart-contracts"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "@nomicfoundation/hardhat-verify": "^2.0.11",
    "hardhat": "^2.22.18"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.5",
    "ethers": "^6.14.0"
  }
}
